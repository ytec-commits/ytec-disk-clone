# Y-TEC Tsumugi Drive 再開引継ぎ（2026-08-03）

## 再開対象

- 正本: `D:\Y-TEC-Workspace\business-apps\ytec-disk-clone`
- 目的: 実機を使わない最終回帰、文書更新、配布ZIP検証を完了する
- 変更しない領域: 物理ディスク、実USB、VMラボの登録・名前・検証済みスナップショット、Microsoft由来ファイルのリポジトリ内配置
- 完成基準: 最新コードと最新ISOでGPT/MBR/VSS/MBR2GPTのVM回帰、全CI、配布ZIP監査が合格し、実機だけの項目を分離して報告できること

## 最初に読むもの

1. `AGENTS.md`
2. `DiskClone_Development_Spec.md`
3. `README.md`
4. この引継ぎ
5. `D:\Y-TEC-Workspace\business-apps\ytec-windows-backup\docs\testing\02-vm-lab.md`

## 現在の重要状態

- Gitリポジトリではない。既存差分を消したり一括整形したりしない。
- VMは直列利用、NIC `none`、実データ不使用。2026-08-03 00時前の確認では稼働中VMは0台。
- 最終候補の通常製品ISO 2011 CA / 2023 CAとMBR2GPT ISOはリポジトリ外に生成済み。
- 最新VM専用ISO 3種もリポジトリ外に生成済みで、実ファイル長とmanifest SHA-256は一致、WIMマウント残留0件。
- 破壊的構成に含まれる非破壊CTestは40/40合格。
- 製品WinPE起動マトリクスはBIOS/UEFI/Secure Bootを含む6/6を目視合格。
- VSS Zstandardバックアップと別ディスク復元後のSecure Boot起動は既存証跡で合格。

## 直前に検出した問題

最新GPT VM回帰:

- 証跡: `.validation\evidence\product-gpt-clone-boot-vm\20260802-232427`
- 製品クローン、BCDBoot、コピー元/ISO切離し、コピー先単独のSecure Boot起動、Windowsデスクトップ到達までは確認。
- ただしWindows Boot Managerに2件のWindows 10エントリが表示された。旧合格証跡にも同じ重複があり、`BCDBoot /c`だけでは本VMの既存BCDを確実に単一化できていない。
- ゲスト厳格プローブは空フィールドのUARTレコードを返したため、元の`result.json`は正しくFAIL。成功へ書き換えない。
- 保護元VDI SHA-256は試験前後とも `D15153558F199C4E06DB6436262CC4C5D162C45BF7C0023E8A109734F0575FF5` で不変。

## 実装中の修正

コピー/復元/MBR2GPTだけが使う `BcdBootStorePolicy::rebuild_fresh` を、単なる`/c`指定から次のトランザクションへ強化中。

1. Microsoft署名済みBCDBootを対象変更前に検証
2. 対象システム領域の固定BCDパスだけを通常ファイル/reparseなしで検査
3. 既存BCDを非上書きの固定退避名へ移動
4. 署名を再検証し、`BCDBoot /c`を実行
5. 新規BCD通常ファイルを確認
6. 成功時は退避を削除、失敗時は部分BCDを除いて旧BCDを復元
7. 単独起動修復の`preserve_existing`は変更しない

変更中ファイル:

- `src/BootRepair/include/ytec/bootrepair/bcdboot.h`
- `src/BootRepair/src/bcdboot.cpp`
- `tests/Unit/boot_repair_tests.cpp`

追加済みテスト観点:

- UEFI/BIOSのBCD固定パス
- 旧BCD退避、新規BCD確認、退避削除
- BCDBoot失敗時の旧BCD復元
- 成功終了でも新規BCDがない場合の失敗と復元
- 退避残留時に実行前停止

直前の単体ビルドは、通常PowerShellから直接`cmake --build`したためWindows SDKの`INCLUDE`が入らず`Windows.h`不在で停止した。コード不合格とはまだ判定していない。次は必ず同じPowerShellプロセスで:

```powershell
. .\scripts\Enter-MsvcEnvironment.ps1
cmake --build --preset msvc-x64 --target ytec-boot-repair-tests
.\out\build\msvc-x64\tests\ytec-boot-repair-tests.exe
```

## 最新生成物

通常製品媒体:

- root: `C:\Users\Lightning\AppData\Local\YTEC\ytec-disk-clone\final-media-candidate\20260802-224639`
- 2011 CA ISO SHA-256: `ED226B6C0C76EDCD48709EFAA89C924FE00CBD490C5B3AE87337E94EAED36AEC`
- 2023 CA ISO SHA-256: `BCB1CC311DE706D4102C050BA8EE0C493940F0F34929FF3A539A5C2077E95C1B`
- MBR2GPT VM-only ISO SHA-256: `B70A15CA38581E146B11B10316025FC38616B31CDB116941C41CFBE2B4405C52`

現在のVM専用自動実行媒体（BCD修正前なので再生成予定）:

- root: `C:\Users\Lightning\AppData\Local\YTEC\ytec-disk-clone\product-boot-validation-media\20260802-231659-final-file`
- VSS復元 ISO SHA-256: `FC17A3640E21EE45DD00EE38E067A6A4B47BA211B8763DEF63215DC89D3B4A6D`
- GPTクローン ISO SHA-256: `2350D0F682A2C2E72F117B36FAC1C03B9D17707249F4EE62950E51444E5759F2`
- MBRクローン ISO SHA-256: `02321D77D109FA62345F9F57701EA50A2483FE02BE0D38B960414F62E12A83FB`

## 次の順番

1. MSVC環境を正しく初期化し、BCD単体テストをビルド/実行
2. 静的VM破壊的構成を再ビルドし、40本の非破壊CTestを実行
3. GPT/MBR/VSS用VM専用ISOをリポジトリ外の新規rootへ再生成
4. GPTコピー先単独Secure Boot回帰を再実行し、BCDメニュー重複が消えたことも目視確認
5. MBRコピー先単独BIOS回帰、VSS復元、必要ならMBR2GPT回帰を直列実行
6. ゲストUART検査の空フィールド問題を、成功扱いへ緩和せず決定的な検査方法へ修正
7. `README.md`、`docs/implementation-status.md`、`docs/test-plan.md`、`docs/release-readiness.md`、`docs/phase5-progress.md`を証跡に合わせて更新
8. 全CI、実ポータブルZIP監査を再実行
9. 正式アイコン未決定、商標/法務、実機マトリクス、コード署名等をリリース前の人間確認として分離
10. 実機以外が本当に完了した場合だけ `G:\マイドライブ\TBDV-0156\アプリ\_zip` へ最終ZIPを置く。現時点では置かない

## 再開用の一文

`D:\Y-TEC-Workspace\business-apps\ytec-disk-clone\docs\handover-20260803.md を読んで、BCD新規再構築トランザクションの単体ビルドから続けてください。実機・物理ディスク・USBは使わず、VMは直列、ZIPは実機以外が完了するまで指定先へ置かないでください。`

## 完了追記（2026-08-03）

この引継ぎの完成基準は達成しました。BCD新規再構築トランザクション、
GPT/MBR/VSS/MBR2GPTの最新製品VM回帰、最新2011/2023 CA ISOの6条件起動、
全CI、最終ポータブルZIP監査の結果は
[`validation-summary-20260803.md`](validation-summary-20260803.md)へ集約しました。
残件は実機/実USB受入と、正式公開時の法務・署名・版番号等の人間判断です。
