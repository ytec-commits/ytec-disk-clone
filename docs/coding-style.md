# コーディング規約

- C++20、Unicode API、RAII、値型を基本とする。
- 所有権は型で表し、Windows `HANDLE` を生の所有状態で保持しない。
- Windows API の失敗はネイティブエラー番号、操作名、利用者向け説明を保持する。
- 可変長 API の返却値、件数、オフセット、長さを使用前に検証する。
- 警告を無効化して通さず、`/W4 /WX` を維持する。
- ログと診断に完全なシリアル、資格情報、ユーザー文書名を出さない。
- Phase を越える先行実装を置かない。

